<!DOCTYPE html>
<html>

<head>
    <title><%= title %></title>
    <link rel="stylesheet" href="css/main.css" />
</head>

<body>
    <%- include('partials/navbar.ejs', { page: currentPage , username: currentUsername }); %>

    <div class="main-content">
        <% body.forEach(function(bodyPart) { %>
            <%- include(bodyPart) %>
        <% }) %>
    </div>

    <%- scripts %>
</body>

</html>
